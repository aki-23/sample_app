.main.main-rank
  .main__groups
    - @groups.each do |group|
      .skyarea.skyarea-rank
        = link_to group_messages_path(group),class: "skyarea_img" do
          = image_tag group.image_url, class: "group-image"
        .skyarea__name
          %i
            = link_to group_messages_path(group) do
              = group.rank
              位
          = link_to group_messages_path(group) do
            = group.name
        -# .skyarea__rank
        -#   = group.rank
        -#   位
= render 'shared/top-btn'